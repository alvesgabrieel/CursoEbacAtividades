// ==> Função UM
const multiplicar = (numeroUm: number, numeroDois: number): any => {
    return numeroUm * numeroDois;
}

console.log(multiplicar(5, 10));

// ==> Função DOIS
const saudacao = (nome: string) => {
    console.log(`Olá ${nome}`) ;
}

saudacao('Gabriel');

